<div class="col-md-12 load-more-container" align="center" data-type="{{$type or 'num'}}" id="{{$id or ''}}" data-total="{{$totalCount or 0}}" data-page="{{$page or 1}}" data-url="{{$url or ''}}">
    <button class="btn btn-default btn-round col-md-3 btn-load-more"> Daha çox </button>
    <div class="col-md-12">
        <div class="loader pagination-loader vertical-align-middle loader-circle" style="display: none;"></div>
    </div>
</div>
